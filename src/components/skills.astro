---
import { Icon } from "astro-icon/components";
import Skill from "./skill.astro";
import { ScrollingSkills } from "./scrolling-skills";
const skillColor = "rgb(217 120 239)";
import { SkillList, type SkillType } from "@/utils/IconMap";
---

<section id="skills">
  <h1 class="text-3xl text-shadow-sm shadow-black mb-0">Meine Fähigkeiten</h1>
  <ScrollingSkills client:only="react">
    {
      SkillList.map((skill: SkillType) => (
        <div class="flex gap-2 border border-slate-200 cursor-grab">
          <Icon name={skill.icon} />
          <div class="flex flex-col">
            <h3 class="">{skill.name}</h3>
            <p>{skill.description}</p>
          </div>
        </div>
      ))
    }
  </ScrollingSkills>
  <div class="skills">
    <ul>
      <Skill title="Linux" value={60} color={skillColor} />
      <Skill title="Web-Dev" value={70} color={skillColor} />
      <Skill title="GUI-Dev (Desktop)" value={30} color={skillColor} />
      <Skill title="Dev-Tools" value={60} color={skillColor} />
      <Skill title="Vibes 😎" value={90} color={skillColor} />
    </ul>
  </div>
</section>

<style>
  section h1 {
    margin-bottom: 2rem;
  }
  section#skills {
    display: flex;
    flex-direction: column;
    gap: 2rem;
    justify-content: center;
    align-items: center;
    padding: 0rem;
    margin: 2rem;
    background-color: var(--color-bg);
    color: var(--color-text);
  }

  .skills {
    width: 100%;
    display: flex;
    flex-direction: column;
    gap: 4rem;
    justify-content: center;
    align-items: center;
  }

  .skills > ul {
    width: 100%;
    display: flex;
    flex-direction: row;
    flex-wrap: wrap;
    justify-content: space-around;
    gap: 1rem;
    list-style: none;
    padding: 0;
  }
</style>
